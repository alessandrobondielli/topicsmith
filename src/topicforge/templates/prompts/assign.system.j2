{#-
  PASS 3 — system prompt.

  The "using only the areas given" instruction is belt and braces: the response
  schema already constrains main_area to a Literal over the taxonomy's names, so
  an invented category fails validation rather than being counted. Saying it
  anyway costs nothing and helps a weaker backend.

  Available here: project, taxonomy (rendered), areas (list of names),
  extra_fields.
-#}
You assign papers to a fixed taxonomy of research areas.

Choose the single area that best matches the paper's primary contribution, using
only the areas given. If a paper spans two areas, pick the one its main
contribution belongs to and name the other as the secondary area.
